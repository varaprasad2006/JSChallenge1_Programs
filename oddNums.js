// print all odd numbers from 1 to 100
for (let i = 1; i <= 100; i++) {
    // Check if the number is odd
    if (i % 2 != 0) {
    // Output the odd number to the browser console
      console.log(i);
    }
  }
