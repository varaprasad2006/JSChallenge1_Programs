let fahrenheit = 45;
 // Function call to get the Celsius equivalent
  let celsius = fahrenheitToCelsius(fahrenheit);
  console.log(celsius + " c");
// Function to fahrenheit to Celsius
const fahrenheitToCelsius = (f) => {
    // Formula
    return ((f - 32) * 5) / 9;
  };
  
