let sum = 0;
      // Loop from 1 to 10
      for (i = 1; i <= 10; i++) {
        // Add the current number (i) to the running sum
        sum=sum+i;
      }
      // Output the sum to the browser console
      console.log("Sum of first 10 numbers is : " + sum);

